<h2><%= @book.title %></h2>
<h3><%= @book.author %></h3>
<h3><%= @book.description %></h3>

<%= link_to "Back",root_path %>
<%= link_to "Edit", edit_book_path(@book) %>
<%= link_to "Delete", book_path(@book), method: :delete, data: {consirm: "Are you sure?"} %>